import React from 'react';

const ViewContainer = () => {
  return (
    <div
      className="viewContainer"
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      }}
    />
  );
};

export default ViewContainer;
